/**
 * When true, the site shows only the maintenance page (no navigation or routes).
 * Set to false when you are ready to publish the full site again.
 */
export const SITE_UNDER_MAINTENANCE = true;
